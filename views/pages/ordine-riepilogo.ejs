<!doctype html>
<html lang="it">
<%- include('../partials/header') %>
<body class="d-flex flex-column min-vh-100">
<%- include('../partials/navbar') %>

<main class="container my-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card shadow-sm">
                <div class="card-body p-lg-5">
                    <h2 class="card-title text-center mb-4">Riepilogo del Tuo Ordine</h2>
                    <div class="alert alert-light" role="alert">
                        Abbiamo simulato l'invio di un'email di conferma a <strong><%= order.buyer.email %></strong> con i dettagli e il link per lasciare una recensione.
                    </div>
                    <hr>
                    <h5 class="mb-3">Prodotti Acquistati</h5>
                    <ul class="list-group list-group-flush mb-4">
                        <% order.items.forEach(item => { %>
                            <li class="list-group-item d-flex justify-content-between align-items-center px-0">
                                <span><%= item.nome %></span>
                                <span class="fw-bold">€<%= (item.prezzo_scontato || item.prezzo).toFixed(2) %></span>
                            </li>
                        <% }) %>
                        <li class="list-group-item d-flex justify-content-between align-items-center px-0 fw-bold fs-5">
                            <span>Totale</span>
                            <span>€<%= order.total.toFixed(2) %></span>
                        </li>
                    </ul>
                    <div class="text-center mt-4">
                        <a href="<%= order.reviewLink %>" class="btn btn-info mb-3">Lascia una recensione ora</a><br>
                        <a href="/" class="btn btn-primary">Continua lo Shopping</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</main>

<div class="modal fade" id="successModal" tabindex="-1" aria-labelledby="successModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-body text-center p-4">
        <i class="bi bi-check-circle-fill text-success" style="font-size: 3rem;"></i>
        <h3 class="mt-3" id="successModalLabel">Grazie!</h3>
        <p class="lead">Il tuo ordine è andato a buon fine.</p>
        <button type="button" class="btn btn-success" data-bs-dismiss="modal">OK</button>
      </div>
    </div>
  </div>
</div>

<%- include('../partials/footer') %>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
    document.addEventListener('DOMContentLoaded', function() {
        var successModal = new bootstrap.Modal(document.getElementById('successModal'));
        successModal.show();
    });
</script>
</body>
</html>